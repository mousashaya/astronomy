[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "desc-demo"
version = "0.1.0"
description = "Weak-lensing dark matter + dark energy demo built on public DES DR1 data"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
